---
slug: print-object-node
title: Pretty print object in node
subtitle: Use console.dir to log all properties of an object
date: 2022-06-12
author: lucca
tags:
  - programming
  - learn-in-public
---

If I simply log a node object with `console.log`, it's nested content quickly gets displayed as `[Object]` or something similar.
In order to see the full content you can use `console.dir(obj, {depth: 10})` where `depth` describes how many hierarchy levels should be printed.

<Replit url="https://replit.com/@idkshite/log-object-node" filename="index.js"/>
